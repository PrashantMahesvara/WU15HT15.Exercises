@model List<MinimalProject.Web.Models.Customer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<ul>
    @foreach (var customer in Model)
    {
        <li>@Html.ActionLink(customer.Name, "Details", new { id = customer.Id })</li>
    }
</ul>